# WARNING: DO NOT modify the version extraction (Extract Version) or changelog extraction (Extract Release Notes) logic.
# These sections are verified and working as intended per user global rules.

name: Build and Release

on:
  push:
    branches:
      - main
    paths:
      - 'CHANGELOG.md'
  workflow_dispatch:

jobs:
  inspect:
    name: Inspect Version
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.get_version.outputs.VERSION }}
      version_tag: ${{ steps.get_version.outputs.VERSION_TAG }}
      skipped: ${{ steps.check_version.outputs.skipped }}
    steps:
      - name: Checkout Code
        uses: actions/checkout@v7

      - name: Extract Version from Changelog
        id: get_version
        run: |
          VERSION=$(sed -n '/## \[/p' CHANGELOG.md | head -n 1 | cut -d'[' -f2 | cut -d']' -f1)
          if [ -z "$VERSION" ]; then
            echo "Error: Could not extract version from CHANGELOG.md"
            exit 1
          fi
          echo "VERSION=$VERSION" >> $GITHUB_OUTPUT
          echo "VERSION_TAG=v$VERSION" >> $GITHUB_OUTPUT
          echo "Detected Version: $VERSION"

      - name: Check If Version Already Released
        id: check_version
        shell: bash
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          TAG="v${{ steps.get_version.outputs.VERSION }}"
          if gh release view "$TAG" >/dev/null 2>&1 && [ "${{ github.event_name }}" != "workflow_dispatch" ]; then
            echo "Version $TAG already exists. Skipping release."
            echo "skipped=true" >> $GITHUB_OUTPUT
          else
            echo "skipped=false" >> $GITHUB_OUTPUT
          fi

  build:
    name: Build and Release
    needs: inspect
    if: needs.inspect.outputs.skipped != 'true'
    runs-on: macos-latest
    permissions:
      contents: write

    steps:
      - name: Checkout Code
        uses: actions/checkout@v7

      - name: Prepare Environment
        run: |
          echo "VERSION=${{ needs.inspect.outputs.version }}" >> $GITHUB_ENV
          echo "VERSION_TAG=${{ needs.inspect.outputs.version_tag }}" >> $GITHUB_ENV

      - name: Set up Xcode
        uses: SteveShi/setup-xcode@v2
        with:
          xcode-version: 'latest-stable'

      - name: Install XcodeGen
        run: |
          brew install xcodegen

      - name: Generate Xcode Project
        run: |
          xcodegen generate --spec project.yml
          
      - name: Resolve Swift Packages
        run: |
          mkdir -p build
          xcodebuild -resolvePackageDependencies -project Essays.xcodeproj -scheme Essays -derivedDataPath build
          

      - name: Build Universal Binary
        run: |
          VERSION_CLEAN=${{ env.VERSION }}
          xcodebuild -scheme Essays \
            -configuration Release \
            -destination 'generic/platform=macOS' \
            -derivedDataPath build \
            MARKETING_VERSION=$VERSION_CLEAN \
            CURRENT_PROJECT_VERSION=${{ github.run_number }} \
            ARCHS="arm64 x86_64" \
            ONLY_ACTIVE_ARCH=NO \
            CODE_SIGNING_ALLOWED=NO \
            build

      - name: Verify App Bundle Version Keys
        run: |
          APP_PATH=$(find build/Build/Products/Release -name "Essays.app" -type d | head -n 1)
          if [ -z "$APP_PATH" ]; then
            echo "CRITICAL ERROR: Essays.app not found!"
            exit 1
          fi
          INFO_PLIST="$APP_PATH/Contents/Info.plist"
          if [ ! -f "$INFO_PLIST" ]; then
            echo "CRITICAL ERROR: Info.plist not found at $INFO_PLIST"
            exit 1
          fi
          SHORT_VERSION=$(/usr/libexec/PlistBuddy -c "Print CFBundleShortVersionString" "$INFO_PLIST" 2>/dev/null || true)
          BUNDLE_VERSION=$(/usr/libexec/PlistBuddy -c "Print CFBundleVersion" "$INFO_PLIST" 2>/dev/null || true)
          if [ -z "$SHORT_VERSION" ] || [ -z "$BUNDLE_VERSION" ]; then
            echo "CRITICAL ERROR: Missing CFBundleShortVersionString or CFBundleVersion in Info.plist"
            exit 1
          fi
          echo "Info.plist versions OK: CFBundleShortVersionString=$SHORT_VERSION, CFBundleVersion=$BUNDLE_VERSION"

      - name: Package App (Universal and arm64)
        run: |
          rm -rf dmg_root deploy Build/Universal Build/arm64
          APP_PATH=$(find build/Build/Products/Release -name "Essays.app" -type d | head -n 1)
          if [ -z "$APP_PATH" ]; then
            echo "CRITICAL ERROR: Essays.app not found!"
            exit 1
          fi
          VERSION_CLEAN=${{ env.VERSION }}
          
          mkdir -p Build/Universal Build/arm64
          cp -R "$APP_PATH" Build/Universal/
          
          # Universal Code Signing
          if [ -d "Build/Universal/Essays.app/Contents/Frameworks/Sparkle.framework" ]; then
            codesign --force --sign - --options=runtime "Build/Universal/Essays.app/Contents/Frameworks/Sparkle.framework/Versions/B/Resources/Autoupdate.app" 2>/dev/null || true
            codesign --force --sign - --options=runtime "Build/Universal/Essays.app/Contents/Frameworks/Sparkle.framework/Versions/B/Helpers/Updater.app" 2>/dev/null || true
            codesign --force --sign - --options=runtime "Build/Universal/Essays.app/Contents/Frameworks/Sparkle.framework/Versions/B" 2>/dev/null || true
          fi
          if [ -f "Resources/Essays.entitlements" ]; then
            codesign --force --deep --sign - --options=runtime --entitlements Resources/Essays.entitlements Build/Universal/Essays.app
          else
            codesign --force --deep --sign - --options=runtime Build/Universal/Essays.app
          fi
          
          # 1. Universal ZIP & DMG
          cd Build/Universal
          zip -qy -r "$GITHUB_WORKSPACE/Essays-${VERSION_CLEAN}-universal.zip" "Essays.app"
          cd "$GITHUB_WORKSPACE"
          
          mkdir -p dmg_root_universal
          cp -R Build/Universal/Essays.app dmg_root_universal/
          hdiutil create -volname "Essays" -srcfolder dmg_root_universal -ov -format UDZO "Essays-${VERSION_CLEAN}-universal.dmg"
          rm -rf dmg_root_universal

          # 2. arm64 Thinning & Signing
          cp -R Build/Universal/Essays.app Build/arm64/
          lipo -thin arm64 Build/arm64/Essays.app/Contents/MacOS/Essays -output Build/arm64/Essays.app/Contents/MacOS/Essays
          if [ -d "Build/arm64/Essays.app/Contents/Frameworks/Sparkle.framework" ]; then
            if lipo -info Build/arm64/Essays.app/Contents/Frameworks/Sparkle.framework/Versions/B/Sparkle 2>/dev/null | grep -q 'x86_64'; then
              lipo -thin arm64 Build/arm64/Essays.app/Contents/Frameworks/Sparkle.framework/Versions/B/Sparkle -output Build/arm64/Essays.app/Contents/Frameworks/Sparkle.framework/Versions/B/Sparkle 2>/dev/null || true
            fi
            codesign --force --sign - --options=runtime "Build/arm64/Essays.app/Contents/Frameworks/Sparkle.framework/Versions/B/Resources/Autoupdate.app" 2>/dev/null || true
            codesign --force --sign - --options=runtime "Build/arm64/Essays.app/Contents/Frameworks/Sparkle.framework/Versions/B/Helpers/Updater.app" 2>/dev/null || true
            codesign --force --sign - --options=runtime "Build/arm64/Essays.app/Contents/Frameworks/Sparkle.framework/Versions/B" 2>/dev/null || true
          fi
          if [ -f "Resources/Essays.entitlements" ]; then
            codesign --force --deep --sign - --options=runtime --entitlements Resources/Essays.entitlements Build/arm64/Essays.app
          else
            codesign --force --deep --sign - --options=runtime Build/arm64/Essays.app
          fi

          # arm64 ZIP & DMG
          cd Build/arm64
          zip -qy -r "$GITHUB_WORKSPACE/Essays-${VERSION_CLEAN}-arm64.zip" "Essays.app"
          cd "$GITHUB_WORKSPACE"
          
          mkdir -p dmg_root_arm64
          cp -R Build/arm64/Essays.app dmg_root_arm64/
          hdiutil create -volname "Essays" -srcfolder dmg_root_arm64 -ov -format UDZO "Essays-${VERSION_CLEAN}-arm64.dmg"
          rm -rf dmg_root_arm64

          echo "ZIP_NAME=Essays-${VERSION_CLEAN}-universal.zip" >> $GITHUB_ENV
          echo "DMG_NAME=Essays-${VERSION_CLEAN}-universal.dmg" >> $GITHUB_ENV

      - name: Extract Release Notes (en & zh)
        run: |
          VERSION_CLEAN=${{ env.VERSION }}
          awk "/^## \[$VERSION_CLEAN\]/{flag=1;next} /^## \[/{flag=0} flag" CHANGELOG.md > full_notes.md
          
          if [ ! -s full_notes.md ]; then
            echo "Release for version $VERSION_CLEAN. Check CHANGELOG.md for details." > release_notes_en.md
            cp release_notes_en.md release_notes_zh.md
          else
            sed -n '1,/### Chinese/p' full_notes.md | grep -v "### Chinese" > release_notes_en.md
            sed -n '/### Chinese/,$p' full_notes.md | sed '1d' > release_notes_zh.md
            
            if [ ! -s release_notes_zh.md ]; then cp release_notes_en.md release_notes_zh.md; fi
            if [ ! -s release_notes_en.md ]; then cp full_notes.md release_notes_en.md; fi
          fi
          cp full_notes.md release_notes.md

      - name: Generate Sparkle Appcast
        env:
          SPARKLE_SECRET: ${{ secrets.SPARKLE_PRIVATE_KEY }}
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          SPARKLE_VERSION=$(gh api repos/sparkle-project/Sparkle/releases/latest --jq '.tag_name' | sed 's/^v//')
          curl -fL -H "User-Agent: curl" -o Sparkle.tar.xz "https://github.com/sparkle-project/Sparkle/releases/download/${SPARKLE_VERSION}/Sparkle-${SPARKLE_VERSION}.tar.xz"
          tar -xf Sparkle.tar.xz

          CLEAN_KEY=$(echo "$SPARKLE_SECRET" | sed 's/EdDSAPrivateKey://' | tr -dc 'A-Za-z0-9+/=')
          
          if [ -z "$CLEAN_KEY" ]; then
            echo "ERROR: SPARKLE_PRIVATE_KEY secret is missing!"
            exit 1
          fi

          mkdir -p deploy
          cp "$GITHUB_WORKSPACE/Essays-${VERSION_CLEAN}-universal.zip" deploy/
          cp release_notes_en.md "deploy/release_notes_en.md"
          cp release_notes_zh.md "deploy/release_notes_zh.md"
          cp release_notes_en.md "deploy/release_notes.md"
          
          export DYLD_FRAMEWORK_PATH="$PWD"
          
          echo "$CLEAN_KEY" | ./bin/generate_appcast \
            --ed-key-file - \
            --download-url-prefix "https://github.com/SteveShi/Essays/releases/download/${{ env.VERSION_TAG }}/" \
            deploy/
          
          if [ -f "deploy/appcast.xml" ]; then
            mv deploy/appcast.xml .
            sed -i.bak '/<description>/d' appcast.xml
            sed -i.bak '/<sparkle:releaseNotesLink>/d' appcast.xml
            
            EN_LINK="https://github.com/SteveShi/Essays/releases/download/${{ env.VERSION_TAG }}/release_notes_en.md"
            ZH_LINK="https://github.com/SteveShi/Essays/releases/download/${{ env.VERSION_TAG }}/release_notes_zh.md"
            
            sed -i.bak "/<enclosure/i\\
            <sparkle:releaseNotesLink xml:lang=\"en\">$EN_LINK</sparkle:releaseNotesLink>" appcast.xml
            sed -i.bak "/<enclosure/i\\
            <sparkle:releaseNotesLink xml:lang=\"zh-Hans\">$ZH_LINK</sparkle:releaseNotesLink>" appcast.xml
            
            rm appcast.xml.bak
            echo "Appcast generated successfully."
          else
            echo "ERROR: generate_appcast failed."
            exit 1
          fi

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v3
        with:
          files: |
            Essays-*-universal.zip
            Essays-*-universal.dmg
            Essays-*-arm64.zip
            Essays-*-arm64.dmg
            release_notes_en.md
            release_notes_zh.md
            appcast.xml
          name: Release ${{ env.VERSION_TAG }}
          tag_name: ${{ env.VERSION_TAG }}
          body_path: release_notes.md
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Update Homebrew Cask
        env:
          HOMEBREW_TAP_TOKEN: ${{ secrets.HOMEBREW_TAP_TOKEN }}
        run: |
          VERSION="${{ env.VERSION }}"
          SHA256=$(shasum -a 256 "${{ env.DMG_NAME }}" | awk '{print $1}')
          
          git clone https://x-access-token:${HOMEBREW_TAP_TOKEN}@github.com/SteveShi/homebrew-tap.git tap-repo
          cd tap-repo
          
          sed -i '' "s/version \".*\"/version \"$VERSION\"/" Casks/essays.rb
          sed -i '' "s/sha256 \".*\"/sha256 \"$SHA256\"/" Casks/essays.rb
          
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add Casks/essays.rb
          git commit -m "Update essays to $VERSION"
          git push
